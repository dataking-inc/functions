import submitEventData from "../src/submitEventData";

/**
 * unfortunately event is not supported for testing so this function cannot be tested
 *
 * @ref https://github.com/airbnb/enzyme/issues/308#issuecomment-215348290
 */

test('assume true', () => {
    expect(true).toBe(true);
});
